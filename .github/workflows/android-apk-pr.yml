name: Android Debug-APK (PR)

on:
  pull_request:
  workflow_dispatch:

jobs:
  build-debug-apk:
    name: Debug-APK bauen
    runs-on: ubuntu-latest
    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: JDK 17 einrichten
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Gradle-Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', '**/gradle.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Android-SDK einrichten
        uses: android-actions/setup-android@v3

      - name: Android-SDK Lizenzen akzeptieren
        run: |
          export ANDROID_SDK_ROOT="${ANDROID_SDK_ROOT:-$ANDROID_HOME}"
          export ANDROID_HOME="${ANDROID_HOME:-$ANDROID_SDK_ROOT}"
          yes | ${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager --licenses

      - name: Android-SDK Pakete installieren
        run: |
          export ANDROID_SDK_ROOT="${ANDROID_SDK_ROOT:-$ANDROID_HOME}"
          export ANDROID_HOME="${ANDROID_HOME:-$ANDROID_SDK_ROOT}"
          ${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager "platforms;android-34" "build-tools;34.0.0" "platform-tools"

      - name: Android local.properties schreiben
        run: |
          echo "sdk.dir=${ANDROID_SDK_ROOT:-$ANDROID_HOME}" > android/local.properties
          echo "BACKEND_BASE_URL=${BACKEND_BASE_URL}" >> android/local.properties
        env:
          BACKEND_BASE_URL: https://example.com

      - name: Gradle-Wrapper-JAR lokal bereitstellen
        if: ${{ hashFiles('android/gradle/wrapper/gradle-wrapper.jar') == '' }}
        run: |
          curl -sSL -o /tmp/gradle-8.7-bin.zip https://services.gradle.org/distributions/gradle-8.7-bin.zip
          rm -rf /tmp/gradle-8.7
          unzip -q /tmp/gradle-8.7-bin.zip -d /tmp
          /tmp/gradle-8.7/bin/gradle -p android wrapper --gradle-version 8.7

      - name: Gradle-Wrapper-JAR prüfen
        run: |
          if [ ! -f android/gradle/wrapper/gradle-wrapper.jar ]; then
            echo "Fehler: android/gradle/wrapper/gradle-wrapper.jar fehlt. Gradle-Wrapper-Erzeugung prüfen." >&2
            exit 1
          fi

      - name: Debug-APK bauen
        working-directory: android
        run: ./gradlew :app:assembleDebug --stacktrace --info --no-daemon

      - name: Debug-APK hochladen
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
